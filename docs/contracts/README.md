# Semantic Contracts

This directory contains per-table semantic contracts documenting the exact meaning
of each KPI measure, grain rules, and aggregation conventions.

One contract per table registered in `data_sources/tables.yml`.

## Contract Template

See `contract_template.md` for the standard contract structure.
