# Active Filter Snippets — Deploy Surface

This directory is the active deploy surface for filter snippets.
Materialized from `instruction_library/corpus/filters/` by `scripts/materialize.py`.

**Do NOT add files here directly.**
