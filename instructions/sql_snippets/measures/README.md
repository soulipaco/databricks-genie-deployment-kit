# Active Measure Snippets — Deploy Surface

This directory is the active deploy surface for measure snippets.
Materialized from `instruction_library/corpus/measures/` by `scripts/materialize.py`.

**Do NOT add files here directly.**
